"use client";

import styled from "styled-components";

export const Hairline = styled.div`
    background: var(--hairline);
    height: 1px;
    width: 100%;
`;

export const VHairline = styled.div`
    background: var(--hairline);
    width: 1px;
    height: 100%;
`;

export default Hairline;
