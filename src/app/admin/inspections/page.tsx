import { AdminInspectionsWrapper } from "@/libs/AdminWrapper";

export const metadata = { title: "Admin · Inspections" };

export default function AdminInspectionsPage() {
	return <AdminInspectionsWrapper />;
}
