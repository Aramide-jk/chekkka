import { AdminInspectorsWrapper } from "@/libs/AdminWrapper";

export const metadata = { title: "Admin · Inspectors" };

export default function AdminInspectorsPage() {
	return <AdminInspectorsWrapper />;
}
