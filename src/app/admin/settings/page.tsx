import { AdminSettingsWrapper } from "@/libs/AdminWrapper";

export const metadata = { title: "Site settings · Chekka" };

export default function AdminSettingsPage() {
	return <AdminSettingsWrapper />;
}
